[tool.ruff]
line-length = 120

[tool.ruff.lint]
# E402: module-level imports not at top — intentional for conditional/lazy imports
# E741: ambiguous variable name — acceptable in limited cases
# F403: import * — used in __init__.py re-exports
ignore = ["E402", "E741", "F403"]
